import { createSlice, createAsyncThunk } from "@reduxjs/toolkit";

const fetchAuthorHome = () => {

}

const AuthorSlice = createSlice({
    name: "Author slice",
    initialState: {},
    reducers: {}

})

export default AuthorSlice.reducer;